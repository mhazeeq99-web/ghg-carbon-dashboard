import './globals.css';

export const metadata = {
  title: 'GHG Carbon Footprint',
  description: 'Scope 1 & Scope 2 carbon footprint data management',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
