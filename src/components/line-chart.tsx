'use client';
export function LineChart({values,label}:{values:number[];label:string}){const max=Math.max(...values,1), min=Math.min(...values,0);const w=900,h=240,p=18;const points=values.map((v,i)=>{const x=p+i*(w-p*2)/(values.length-1);const y=h-p-((v-min)/(max-min||1))*(h-p*2);return [x,y]});const d=points.map((p,i)=>(i?'L':'M')+` ${p[0]} ${p[1]}`).join(' ');return <div className="chart"><svg viewBox={`0 0 ${w} ${h}`} preserveAspectRatio="none"><line x1="18" x2="882" y1="222" y2="222" stroke="#e5e9eb"/><path d={d} fill="none" stroke="#0b6b4f" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round"/>{points.map((p,i)=><circle key={i} cx={p[0]} cy={p[1]} r="3.5" fill="#0b6b4f"/>)}<text x="18" y="238" fontSize="10" fill="#69757d">Jan</text><text x="450" y="238" fontSize="10" fill="#69757d" textAnchor="middle">Jun</text><text x="882" y="238" fontSize="10" fill="#69757d" textAnchor="end">Dec</text></svg><div className="legend"><span><i className="dot"/> {label}</span></div></div>}
